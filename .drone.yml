pipeline:
  test:
    image: node:8
    commands:
      - npm install
      - npm run lint
      - npm test

  slack:
    image: plugins/slack
    username: drone
    secrets: [slack_webhook]
    when:
      event: [pull_request]
