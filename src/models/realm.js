/**
 * The realm model
 *
 * @param {string} id
 * @param {string} name
 * @param {string} tenantId
 * @return {{id: string, name: string, tenantId: string}}
 */
module.exports = ({
  id,
  name,
  tenantId,
}) => ({
  id,
  name,
  tenantId,
});
