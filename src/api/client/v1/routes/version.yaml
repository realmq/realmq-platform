/version:
  get:
    security: []
    operationId: fetchVersion
    responses:
      200:
        description: Current version, see https://semver.org for format.
        schema:
          type: object
          properties:
            version:
              type: string
          required:
            - version
        examples:
          version: 1.0.0
