/version:
  get:
    security: []
    operationId: fetchVersion
    responses:
      200:
        description: Current version, see https://semver.org for format.
        schema:
          type: string
